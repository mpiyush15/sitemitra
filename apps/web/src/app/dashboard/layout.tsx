import { PlatformBusinessLayout } from "@/components/layout/platform-business-layout";

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <PlatformBusinessLayout>{children}</PlatformBusinessLayout>;
}
